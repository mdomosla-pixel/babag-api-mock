(ns babag.api.config)

(def api {:bind-address "0.0.0.0"
          :port         9090})

(def query {:bind-address "0.0.0.0"
            :port         9191})

(def admin {:enabled false})

(def aws {})
